import React from 'react';

import css from './Spinner.module.css';

const Spinner = (props) => {
  return <div className={css.Spinner}>Loading...</div>;
};

Spinner.propTypes = {};

export default Spinner;
